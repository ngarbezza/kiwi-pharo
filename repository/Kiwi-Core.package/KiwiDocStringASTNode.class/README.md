I am a node of the Gherkin AST which represents a literal string that can be passed as an optional argument to a step.

Example:

Given some step
	"""
	I store the contents
	of this string AS IS.
	"""